# Distributed Event Aggregator (v2.1.0)

[🇺🇸 English](README.md) | [🇷🇺 Русский](README.ru.md)

**Distributed Event Aggregator** — это централизованная система мониторинга инфраструктуры, разработанная для высоконагруженного приема, обработки и визуализации распределенных потоков событий. Система предоставляет администраторам полную оперативную картину (COP) в реальном времени, включая анализ тональности (sentiment analysis) и обнаружение аномалий.

![System Status](https://img.shields.io/badge/Status-Operational-success?style=flat-square) ![Version](https://img.shields.io/badge/Version-v2.1.0-blue?style=flat-square) ![License](https://img.shields.io/badge/License-MIT-gray?style=flat-square)

## Ключевые Возможности

### Инфраструктура
*   **Микросервисная Архитектура**: Слабая связность компонентов через Apache Kafka.
*   **Высокопроизводительный Ingest**: Go-продюсер для приема событий с минимальной задержкой.
*   **Интеллектуальная Обработка**: Python-консьюмер с анализом тональности (NLTK).
*   **Надежное Хранение**: Шардированный кластер MongoDB.

### Продвинутый Дашборд (v2.1.0)
*   **Корпоративный UI/UX**: Чистый, "строгий" стиль с использованием TailwindCSS.
*   **Безопасный Доступ**: Симуляция RBAC с экраном входа и управлением сессиями.
*   **Интерактивные Инструменты**:
    *   **Глубокий Поиск**: Фильтрация локального буфера событий в реальном времени.
    *   **Конфигурация Системы**: Пауза/Возобновление потока данных и управление уведомлениями.
    *   **Центр Документации**: Встроенное модальное окно со спецификациями API и диаграммами.

## Технологический Стек

*   **Frontend**: React 18, Vite, TailwindCSS, Recharts, Lucide Icons.
*   **Backend**: Go (Gin), Python (Kafka-Python), Node.js (Express).
*   **Брокер Сообщений**: Apache Kafka + Zookeeper.
*   **База Данных**: MongoDB (v7.0).
*   **Контейнеризация**: Docker & Docker Compose.

## Быстрый Старт

### Требования
*   Установленные Docker и Docker Compose.

### Установка

1.  **Клонирование и Запуск**
    ```bash
    git clone https://github.com/your-repo/distributed-event-aggregator.git
    cd distributed-event-aggregator
    docker-compose up --build -d
    ```

2.  **Инициализация**
    Подождите около 30 секунд для создания топиков Kafka и стабилизации сервисов.

3.  **Доступ к Дашборду**
    Откройте [http://localhost:3100](http://localhost:3100)

    > **Данные для входа:**
    > *   **Логин**: `admin`
    > *   **Пароль**: `password` (или любая непустая строка)

## Руководство Пользователя

### 1. Генерация Трафика
Вы можете симулировать входящие события с помощью включенного Python-скрипта или через cURL.

**Автоматический Нагрузочный Тест:**
```bash
python test_load.py
```
*Этот скрипт генерирует случайные события с разной тональностью и метаданными.*

**Ручная Отправка:**
```bash
curl -X POST http://localhost:8090/ingest \
  -H "Content-Type: application/json" \
  -d '{"source":"manual-test", "payload":{"text":"System performance is degrading rapidly.", "meta": "critical"}}'
```

### 2. Управление Дашбордом
*   **Поиск**: Нажмите на иконку лупы (или `/`), чтобы фильтровать события по источнику или содержимому.
*   **Настройки**: Перейдите в **Профиль -> Settings**, чтобы приостановить живой поток или переключить уведомления.
*   **Документация**: Нажмите "Documentation" в верхней панели для просмотра деталей API.

## Справочник API

### Прием События (Ingest)
**POST** `/ingest`
```json
{
  "source": "service-name",
  "payload": {
    "text": "Сообщение лога или отзыв пользователя",
    "user_id": 12345,
    "meta": "optional-metadata"
  }
}
```

### Поток Событий (Stream)
**GET** `/api/events/stream`
*SSE (Server-Sent Events) эндпоинт для обновления дашборда в реальном времени.*

## Устранение Неполадок

*   **Сервисы не запускаются?** Проверьте логи: `docker-compose logs -f`
*   **Нет данных на дашборде?** Убедитесь, что настройка "Feed Paused" выключена в профиле пользователя.
